# This is a Databricks asset bundle definition for earthquake_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: earthquake_bundle
  uuid: fe04b536-274f-4692-82a4-9bb14b7708b9

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use
  WarehouseID:
    description: WarehouseID

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405608584849966.6.azuredatabricks.net
    variables:
      catalog: dev
      schema: dev
      WarehouseID: bdb6653a167a60ce
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://adb-7405608584849966.6.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/101577776@georgebrown.ca to make sure we only have a single copy.
      root_path: /Workspace/Users/101577776@georgebrown.ca/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: prod
      schema: prod
      WarehouseID: bdb6653a167a60ce
    permissions:
      - user_name: 101577776@georgebrown.ca
        level: CAN_MANAGE
